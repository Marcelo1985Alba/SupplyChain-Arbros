@inherits PreciosDialogBase

<SfSpinner @ref="@refSpinner" Size="80"
           Label="" Type="SpinnerType.Fabric">
</SfSpinner>

<BuscadorEmergente Height="500px" TItem="PreciosArticulos" Titulo="Listado de Precios" Visible="@PopupBuscadorVisible"
                   DataSource="@datasource" IsModal="true" Width="90%" HeightGrid="150px"
                   Columnas="@(new[] { "ID", "DESCRIPCION", "PRECIO", "MONEDA" })"
                   OnObjetoSeleccionado="SendObjectSelected"
                   OnCerrarDialog="CerrarDialog"/>