@inherits ProductoDialogBase

<SfSpinner @ref="@refSpinner" Size="80"
           Label="" Type="SpinnerType.Fabric">
</SfSpinner>

<BuscadorEmergente Height="500px" TItem="Producto" Titulo="Listado de Productos y Reparaciones" Visible="@PopupBuscadorVisible"
                   DataSource="@productos" IsModal="true" Width="90%" HeightGrid="250px"
                   Columnas="@(new[] { "ID", "DES_PROD" })"
                   OnObjetoSeleccionado="SendObjectSelected"
                   OnCerrarDialog="CerrarDialog"/>